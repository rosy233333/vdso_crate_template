[package]
name = "api"
version.workspace = true
edition.workspace = true

[dependencies]
xmas-elf = "0.9.0"
structs = { workspace = true }
log = { version = "0.4", optional = true }

[build-dependencies]
regex = "1.8.5"

[features]
log = ["dep:log"]
default = []
